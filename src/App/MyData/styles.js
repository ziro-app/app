import { maxWidth, standardPadding } from '../../Theme/styleVariables'

export const

container = {
	display: 'grid',
	gridRowGap: '20px',
	margin: '60px auto',
	maxWidth: maxWidth,
	padding: standardPadding,
	textAlign: 'center'
}