import { maxWidth, fontTitle } from '../../../Theme/styleVariables'

export const

container = {
	display: 'grid',
	gridTemplateColumns: '30px 1fr',
	placeItems: 'center',
	maxWidth: maxWidth,
	margin: '0 auto'
},

text = {
	marginLeft: '-30px',
	fontFamily: fontTitle
}